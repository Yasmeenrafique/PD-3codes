#include<iostream>
using namespace std;
main()
{
  int num,sum;
  cout<<"Enter the  number1: ";
  cin>>num;
  sum=num;
  cout<<"Enter the  number2: ";
  cin>>num;
  sum=sum+num;
  cout<<"Enter the  number3: ";
  cin>>num;
  sum=sum+num;
  cout<<"Enter the  number4: ";
  cin>>num;
  sum=sum+num;
  cout<<"Enter the  number5: ";
  cin>>num;
  sum=sum+num;
  cout<<"The sum of five integers is:"<<sum;
}